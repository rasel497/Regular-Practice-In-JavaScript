
const dew = 35;
const cocola = 40;
const sprite = 45;

// using Math.max
const maxNumber = Math.max(dew, cocola, sprite);
console.log('max number', maxNumber);

// using math.min
const minNnumber = Math.min(dew, cocola, sprite);
console.log('min number', minNnumber);



// using if else

/*
if (dew > cocola && dew > sprite) {
    console.log('dew is Max winner');
}

else if (cocola > dew && cocola > sprite) {
    console.log('cocola is Max winner');
}

else {
    console.log('sprite is Max winner');
}
**/